
<% if model.errors.any? %>
<div id="error_explanation">
  <h2><%= pluralize(model.errors.count, "error") %> prohibited the execution:</h2>

  
    <% model.errors.each do |error| %>
      <%= error.full_message %>  | 
    <% end %>
  
</div>
<% end %>